server:
  port: 8088

spring:
  application:
    name: 'auth'
  boot:
    admin:
      client:
        url:
        - https://microservice-boot-admin.herokuapp.com

management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
  endpoint:
    health:
      show-details: always